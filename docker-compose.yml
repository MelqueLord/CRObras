services:
  postgres:
    image: postgres:16-alpine
    container_name: crobras-postgres
    environment:
      POSTGRES_DB: crobras
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
